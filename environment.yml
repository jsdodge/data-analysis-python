# Create the conda environment with the following statement:
# conda env create -f environment.yml
name: pydata
channels:
  - conda-forge
dependencies:
  - jupyter
  - jupyterlab
  - matplotlib
  - notebook
  - numpy
  - python
  - scipy
